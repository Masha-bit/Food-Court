import React from 'react'

export default function BottomNavItems(props) {
  return (
    <div>
        {/* <Link href={props.itemLink}> */}
        <div>
            {props.itemIcon}
        </div>
        {/* </Link> */}
    </div>
  )
}
